import "./style.css";
export const metadata={title:"Second Opinion",description:"Before you pay it, sign it, or accept it — check it."};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}